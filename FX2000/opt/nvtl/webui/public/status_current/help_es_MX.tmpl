<!-- Internet  Status -->
<div id="status_current">
	<h1><tmpl_var _("webui", "status_current_menu_text")></h1>

	<h3><tmpl_var truncate(_("webui", "status_current_connection_status_label"), (size(_("webui", "status_current_connection_status_label")) - size(":")))></h3>
	<p>Esta muestra el estado de conexión para la conexión a Internet por medio de la red móvil. </p>
	<h3><tmpl_var truncate(_("webui", "status_current_network_name_label"), (size(_("webui", "status_current_network_name_label")) - size(":")))></h3>
	<p>El nombre de la red móvil a la cual este dispositivo está conectado actualmente. </p>
	<h3><tmpl_var truncate(_("webui", "status_current_technology_label"), (size(_("webui", "status_current_technology_label")) - size(":")))></h3>
	<p>La tecnología de red utilizada para la conexión actual. LTE es la tecnología que más rápidamente está disponible. </p> 
	<!--                    
	<h3>APN</h3>
	<p>The Access Point Name (APN) is pre-configured for the Verizon network, and cannot be changed.
	</p>    
	-->
        <h3><tmpl_var truncate(_("webui", "status_current_connection_time_label"), (size(_("webui", "status_current_connection_time_label")) - size(":")))></h3>
	<p>Para la sesión de Internet actual, el período transcurrido desde que se estableció la conexión. </p>
	<h3><tmpl_var truncate(_("webui", "status_current_received_label"), (size(_("webui", "status_current_received_label")) - size(":")))>
</h3>
	<p>Para la sesión actual de Internet, la cantidad de datos recibidos. Este contador empieza de cero cuando se establece la conexión. </p>
	<h3><tmpl_var truncate(_("webui", "status_current_transmitted_label"), (size(_("webui", "status_current_transmitted_label")) - size(":")))></h3>
	<p>Para la sesión actual de Internet, la cantidad de datos transmitidos. Este contador empieza de cero cuando se establece la conexión. </p>

	<h2><tmpl_var _("webui", "status_current_sub_header_ipv4_text")></h2>
	<h3><tmpl_var truncate(_("webui", "status_current_ipv4_label"), (size(_("webui", "status_current_ipv4_label")) - size(":")))></h3>
	<p> La dirección de IP de Internet asignada a este dispositivo. </p>
	<h3><tmpl_var truncate(_("webui", "status_current_ipv4mask_label"), (size(_("webui", "status_current_ipv4mask_label")) - size(":")))>
</h3>
	<p> La máscara de red asociada con la dirección de IP anterior. </p>
	<h3><tmpl_var truncate(_("webui", "status_current_ipv4gateway_label"), (size(_("webui", "status_current_ipv4gateway_label")) - size(":")))></h3>
	<p> La dirección de IP de la pasarela asociada con la dirección de IP anterior. </p>
	<h3><tmpl_var truncate(_("webui", "status_current_ipv4dns_label"), (size(_("webui", "status_current_ipv4dns_label")) - size(":")))>
</h3>
	<p> El servidor del nombre de dominio actualmente usado por este dispositivo. </p>

	<h2><tmpl_var _("webui", "status_current_sub_header_ipv6_text")></h2>
	<h3><tmpl_var truncate(_("webui", "status_current_ipv6global_label"), (size(_("webui", "status_current_ipv6global_label")) - size(":")))></h3>
	<p>La dirección de IPv6 global para el <tmpl_var _("webui", "webui_product_name")> se muestra aquí. Quedará en blanco si el IPv6 está desactivado o si no está admitido por la conexión de red actual. </p>

	<tmpl_if InternetStatusViewSessionShow>
	<h2><tmpl_var _("webui", "status_current_internet_session_btn")></h2>
	<p>El registro de la sesión contiene los siguientes datos:</p>
	<p>- Fecha y hora   (cuándo comenzó la conexión)</p>
	<p>- Duración (con el mismo formato que la información de sesión actual)</p>
	<p>- Recibido    (formato en cuanto a la sesión actual)</p>
	<p>- Transmitido    (formato en cuanto a la sesión actual)</p>
	<p>- Datos totales  (suma de Rx y Tx, formato en cuanto a la información de sesión actual)</p>
	<p>- Roaming   (Roaming/Parcial/en blanco) En blanco=red doméstica</p>
	<p>- IPv4   (Dirección IP IPv4 WWAN)</p>
	<p>- IPv6   (Dirección IP IPv6 WWAN)</p>

	<p>El registro está en orden de fecha. </p>
	<p>El usuario no puede cambiar el orden y no existen opciones relacionadas con el registro de sesión.</p>
	</tmpl_if>

</div> 
<!-- id="status_internet" -->

