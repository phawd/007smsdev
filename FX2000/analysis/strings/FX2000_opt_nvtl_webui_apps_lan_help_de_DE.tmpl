<!-- LAN -->
<div id="lan">
	<h1><tmpl_var _("webui", "adv_lan_menu_text")></h1>
bersicht</h2>
	<p>Diese Einstellungen gelten f
r Ihr lokales Netzwerk oder LAN. F
r dieses Ger
t besteht das LAN aus diesem Ger
t und allen verbundenen Ger
ten (einschlie
lich der 
ber Ethernet verbundenen Ger
te, sofern vorhanden).</p>
	<tmpl_if showIPPTEnable>
	<h3>IP-Passthrough</h3>
            Die Funktion 
IP-Passthrough
 weist, wenn sie aktiviert ist, einem verbundenen Ger
t die Internet-IP-Adresse zu.
    </p>
    <ul>
            <li>
ber die ausgew
hlte Schnittstelle darf au
er dem Passthrough-Ger
t kein weiteres Ger
t verbunden sein. Andere Ger
te an dieser Schnittstelle werden ignoriert und haben keinen Internetzugang.
            </li>
            <li>Ger
te an anderen Schnittstellen funktionieren normal.
            </li>
            <li>Wenn IP-Passthrough aktiviert ist, k
nnen die folgenden Funktionen nicht verwendet werden:
            <ul>
                    <li>Portweiterleitung </li>
                    <li>Portfilterung </li>
                    <li>DMZ </li>
            </ul>
    </li></ul>
    <li>
            Wenn IP-Passthrough aktiviert ist, muss das <tmpl_var _("webui", "webui_product_name")> neu gestartet werden. Das bedeutet in der Regel auch, dass dem <tmpl_var _("webui", "webui_product_name")> nach dem Neustart eine neue Internet-IP-Adresse zugewiesen wird.
    </li>
    <h3>IP-Passthrough aktivieren</h3>
            Dar
sst sich die Funktion nach Bedarf aktivieren oder deaktivieren.
    </p>
    <tmpl_if showIPPTInterface>
    <h3>Passthrough-Schnittstelle</h3>
            W
hlen Sie die Schnittstelle aus, die von dem Netzwerkger
t verwendet wird, das als IP-Passthrough-Ger
t fungieren soll.
            HINWEIS: Wenn IPPT (IP-Passthrough) an einer Schnittstelle aktiviert ist, werden alle anderen Schnittstellen deaktiviert.
    </p>
    </tmpl_if>
    </tmpl_if>
    <h2><tmpl_var _("webui", "adv_lan_ip4_sub_header_text")></h2>
	<tmpl_if LANIPAddressShow>			
	<h3><tmpl_var truncate(_("webui", "adv_lan_ip4_address_label"), (size(_("webui", "adv_lan_ip4_address_label")) - size(":")))></h3>
			Die IP-Adresse f
r dieses Ger
t, vom lokalen Netzwerk aus betrachtet.
			Normalerweise k
nnen Sie den Standardwert verwenden.
	</tmpl_if>
	<tmpl_if LANSubnetMaskShow>
	<h3><tmpl_var truncate(_("webui", "adv_lan_ip4_subnet_mask_label"), (size(_("webui", "adv_lan_ip4_subnet_mask_label")) - size(":")))></h3>
			Der Standardwert 255.255.255.0 ist Standard f
r kleine Netzwerke (Klasse 
			Wenn Sie die LAN-IP-Adresse 
ndern, stellen Sie sicher, dass Sie die richtige Subnetzmaske f
r den IP-Adressbereich der LAN-IP-Adresse verwenden.
	</tmpl_if>
	<tmpl_if LANMacAddressShow>	
	<h3><tmpl_var truncate(_("webui", "adv_lan_ip4_mac_address_label"), (size(_("webui", "adv_lan_ip4_mac_address_label")) - size(":")))></h3>
			Die MAC-Adresse ist eine eindeutige Netzwerkkennung, die bei der Herstellung eines Netzwerkger
ts zugewiesen wird.
			In diesem schreibgesch
tzten Feld wird die MAC-Adresse f
r die Schnittstellen dieses Ger
ts angezeigt.
	</tmpl_if>
	<tmpl_if LANDHCPEnableShow>
	<h3><tmpl_var truncate(_("webui", "adv_lan_ip4_dhcp_enable_label"), (size(_("webui", "adv_lan_ip4_dhcp_enable_label")) - size(":")))></h3>
		<p>Es empfiehlt sich, diese Option nicht zu deaktivieren. Der DHCP-Server weist jedem verbundenen Ger
t eine IP-Adresse zu. W
rde der DHCP-Server ausgeschaltet, m
sste jedem verbundenen Ger
t eine feste IP-Adresse zugewiesen werden.
	</tmpl_if>
	<tmpl_if LANLANDHCPLeaseTimeShow>
	<h3><tmpl_var truncate(_("webui", "adv_lan_ip4_dhcp_lease_time_label"), (size(_("webui", "adv_lan_ip4_dhcp_lease_time_label")) - size(":")))></h3>
		<p>Die Dauer in Minuten, nach der verbundene Ger
te die ihnen vom DHCP-Server zugewiesene IP-Adresse erneuern m
ssen. Normalerweise k
nnen Sie den Standardwert beibehalten. Wenn Sie jedoch spezielle Anforderungen haben, k
nnen Sie diesen Wert 
	</tmpl_if>
	<tmpl_if LANDHCPStartAddressShow>
	<h3><tmpl_var truncate(_("webui", "adv_lan_ip4_dhcp_range_begin_label"), (size(_("webui", "adv_lan_ip4_dhcp_range_begin_label")) - size(":")))></h3>
		<p>Der Anfang des IP-Adressbereichs, der vom DHCP-Server verwendet wird. Wenn die IP auf dem Clientger
t festgelegt ist, verwenden Sie eine IP-Adresse au
erhalb dieses DHCP-Bereichs. Wird die IP-Adresse mithilfe einer IP-Reservierung festgelegt, liegt sie normalerweise innerhalb dieses Bereichs. HINWEIS: Nur erfahrene Benutzer sollten diese Einstellung 
	</tmpl_if>
	<tmpl_if LANDHCPEndAddressShow>
	<h3><tmpl_var truncate(_("webui", "adv_lan_ip4_current_dhcp_range_label"), (size(_("webui", "adv_lan_ip4_current_dhcp_range_label")) - size(":")))></h3>
		<p>Das Ende des IP-Adressbereichs, der vom DHCP-Server verwendet wird. Wenn die IP auf dem Clientger
t festgelegt ist, verwenden Sie eine IP-Adresse au
erhalb dieses DHCP-Bereichs. Wird die IP-Adresse mithilfe einer IP-Reservierung festgelegt, liegt sie normalerweise innerhalb dieses Bereichs. HINWEIS: Nur erfahrene Benutzer sollten diese Einstellung 
	</tmpl_if>
	<tmpl_if LANReservedIPAddressShow>		
	<h3><tmpl_var truncate(_("webui", "adv_lan_reservedIp_button"), (size(_("webui", "adv_lan_reservedIp_button")) - size(":")))></h3>
ber diese Funktion k
nnen Sie sicherstellen, dass ein verbundenes Ger
t vom <tmpl_var _("webui", "webui_product_name")> immer die gleiche IP-Adresse zugewiesen bekommt. Klicken Sie auf diese Schaltfl
che, um den Einrichtungsbildschirm f
r diese Funktion aufzurufen.
	</tmpl_if>
	<tmpl_if ipv6Supported>		
		<h2><tmpl_var _("webui", "adv_lan_ip6_sub_header_text")></h2>
		<tmpl_if showIPv6EnableOption>
		<h3><tmpl_var truncate(_("webui", "adv_lan_ip6_enable_label"), (size(_("webui", "adv_lan_ip6_enable_label")) - size(":")))></h3>
			<p>Wenn einige der verbundenen Ger
te IPv6 unterst
tzen, sollten Sie diese Einstellung aktivieren.
ber IPv6 verbundene Ger
nnen dar
ber IPv6-Verbindungen mit dem Internet herstellen.
		</tmpl_if>	
		<h3><tmpl_var truncate(_("webui", "adv_lan_ip6_link_local_label"), (size(_("webui", "adv_lan_ip6_link_local_label")) - size(":")))></h3>
			<p>Das ist die IPv6-Adresse dieses Ger
ts. Die verbindungslokale Adresse wird f
r die IPv6-Kommunikation zwischen Ger
ten im gleichen Netzwerk verwendet. </p>
	</tmpl_if>
	<tmpl_if showLANDeviceName>
                <h2><tmpl_var _("webui", "adv_lan_web_management_header_text")></h2>
                <h3><tmpl_var truncate(_("webui", "adv_lan_url_name_label"), (size(_("webui", "adv_lan_url_name_label")) - size(":")))></h3>
                <p>Der auf der Seite 
Benutzereinstellungen
 eingetragene Ger
tename bildet die Grundlage f
r die Zugriffs-URL auf die lokale Webverwaltungs-Schnittstelle f
r das Ger
        </tmpl_if>
<!-- id="lan" -->
