<!-- Internet  Status -->
<div id="status_current">
	<h1><tmpl_var _("webui", "status_current_menu_text")></h1>

	<h3><tmpl_var truncate(_("webui", "status_current_connection_status_label"), (size(_("webui", "status_current_connection_status_label")) - size(":")))></h3>
	<p>This displays the connection status for the internet connection via the mobile network. 
	</p>
	<h3><tmpl_var truncate(_("webui", "status_current_network_name_label"), (size(_("webui", "status_current_network_name_label")) - size(":")))></h3>
	<p>The name of the mobile network to which this device is currently connected. 
	</p>
	<h3><tmpl_var truncate(_("webui", "status_current_technology_label"), (size(_("webui", "status_current_technology_label")) - size(":")))></h3>
	<p>The network technology used for the current connection. LTE is the fastest available technology. 
	</p> 
	<!--                    
	<h3>APN</h3>
	<p>The Access Point Name (APN) is pre-configured for the Verizon network, and cannot be changed.
	</p>    
	-->
        <h3><tmpl_var truncate(_("webui", "status_current_connection_time_label"), (size(_("webui", "status_current_connection_time_label")) - size(":")))></h3>
	<p>For the current internet session, the period of time that has elapsed since the connection was established. 
	</p>
	<h3><tmpl_var truncate(_("webui", "status_current_received_label"), (size(_("webui", "status_current_received_label")) - size(":")))>
</h3>
	<p>For the current internet session, the amount of data received. This counter starts from zero when the connection is established. 
	</p>
	<h3><tmpl_var truncate(_("webui", "status_current_transmitted_label"), (size(_("webui", "status_current_transmitted_label")) - size(":")))></h3>
	<p>For the current internet session, the amount of data transmitted. This counter starts from zero when the connection is established. 
	</p>

	<h2><tmpl_var _("webui", "status_current_sub_header_ipv4_text")></h2>
	<h3><tmpl_var truncate(_("webui", "status_current_ipv4_label"), (size(_("webui", "status_current_ipv4_label")) - size(":")))></h3>
	<p>
        	The internet IP address assigned to this device.
	</p>
	<h3><tmpl_var truncate(_("webui", "status_current_ipv4mask_label"), (size(_("webui", "status_current_ipv4mask_label")) - size(":")))>
</h3>
	<p>
	        The network mask associated with the IP address above.
	</p>
	<h3><tmpl_var truncate(_("webui", "status_current_ipv4gateway_label"), (size(_("webui", "status_current_ipv4gateway_label")) - size(":")))></h3>
	<p>
	        The gateway IP address associated with the IP address above.
	</p>
	<h3><tmpl_var truncate(_("webui", "status_current_ipv4dns_label"), (size(_("webui", "status_current_ipv4dns_label")) - size(":")))>
</h3>
	<p>
	        The Domain Name Server currently used by this device.
	</p>

	<h2><tmpl_var _("webui", "status_current_sub_header_ipv6_text")></h2>
	<h3><tmpl_var truncate(_("webui", "status_current_ipv6global_label"), (size(_("webui", "status_current_ipv6global_label")) - size(":")))></h3>
	<p>The global IPv6 address for the <tmpl_var _("webui", "webui_product_name")> is displayed here. 
	This will be blank if IPv6 is turned off, or is not supported by the current network connection. </p>

	<h2><tmpl_var _("webui", "status_current_internet_session_btn")></h2>
	<p>The session log contains the following data:</p>
	<p>- Date/Time   (when connected started)</p>
	<p>- Duration  (same format as current session info)</p>
	<p>- Received Data (same format as current session)</p>
	<p>- Transmitted Data (same format as current session)</p>
	<p>- Total Data  (sum of Rx and Tx, format as for current session info)</p>
	<p>- Roaming Status  (Roaming/Partial/blank) Blank=home network</p>
	<p>- IPv4 Address  (IPv4 WWAN IP address)</p>
	<p>- IPv6 Address  (IPv6 WWAN IP address)</p>

	<p><strong>Note: </strong>Internet Session logs are shown in date order.</p>
	

</div> 
<!-- id="status_internet" -->

